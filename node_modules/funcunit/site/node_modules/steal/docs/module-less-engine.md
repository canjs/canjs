@typedef {Object} @less-engine
@parent StealJS.modules

@option {Object}
The [LESS compiler](http://lesscss.org/#using-less) which is used by the
`$less` module to compile modules with `.less!` extension.
