<ol>
<% ordered.each(function(item) { %>
	<li>
		<% if(item.ul) { %>
		<%== can.view.render(can.test.path('src/view/ejs/test/indirect1.ejs'), { unordered: item.ul }) %>
		<% } else { %>
		<%= item.toString() %>
		<% } %>
	</li>
<% }) %>
</ol>