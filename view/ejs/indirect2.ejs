<ol>
<% ordered.each(function(item) { %>
	<li>
		<% if(item.ul) { %>
		<%== can.view.render('indirect1.ejs', { unordered: item.ul }) %>
		<% } else { %>
		<%= item.toString() %>
		<% } %>
	</li>
<% }) %>
</ol>