<ul>
<% unordered.each(function(item) { %>
	<li>
		<% if(item.ol) { %>
		<%== can.view.render('//can/view/ejs/indirect2.ejs', { ordered: item.ol }) %>
		<% } else { %>
		<%= item.toString() %>
		<% } %>
	</li>
<% }) %>
</ul>