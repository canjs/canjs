steal.apps('jquery').then(function($){
	/**
	 * Mixes in other plugins by name and optional arguments
	 * 
	 * .mixin(['jupiter',[]],[])
	 */
	$.fn.mixin = function(){
		var args = $.makeArray(arguments);
		//check if this is an array for a single controller
		
		
	}
});