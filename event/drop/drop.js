include.apps('jquery/events/drag').then(function(){
	
	//somehow need to keep track of elements with selectors on them.  When element is removed, somehow we need to know that
	//
	event.setupHelper( ["dropover","dropon","dropout","dropinit","dropmove","dropend"], function(e){
		
		
	} )
	
});